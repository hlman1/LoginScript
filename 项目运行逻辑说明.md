# Steam 批量登录工具 - 完整运行逻辑说明

## 📋 项目概述

**项目名称**: Steam 账号批量登录工具
**版本**: v2.3 (窗口检测版)
**功能**: 自动登录多个 Steam 账号，启动 PUBG 游戏，运行指定时间后退出

---

## 🔄 完整运行流程

### 1️⃣ 启动阶段

```
用户操作
  ↓
双击 start.bat
  ↓
启动 GUI (steam_login_gui.py)
  ↓
用户点击 "开始运行"
  ↓
调用 login_steam.py 的 main() 函数
```

### 2️⃣ 初始化阶段

```python
# 1. 创建主实例
bot = SteamLoginBatch('accounts.txt')

# 2. 加载账号文件
bot.load_accounts()
  ├─ 读取 accounts.txt
  ├─ 解析 Tab 分隔的账号信息
  └─ 返回账号列表

# 3. 验证账号
if len(bot.accounts) > 0:
    # 继续执行
else:
    # 提示并退出
```

### 3️⃣ 账号循环阶段

```python
# 对每个账号执行完整流程
for i, account in enumerate(bot.accounts, 1):
    print(f"📋 进度: [{i}/{total}]")
    print(f"📋 处理账号: {account['username']}")

    # 执行完整的 PUBG 流程
    bot.visit_pubg_page(account)

    # 账号间等待
    time.sleep(2)
```

### 4️⃣ 单个账号完整流程 (visit_pubg_page)

这是**核心流程**，每个账号都会执行以下步骤：

```
┌─────────────────────────────────────────┐
│  步骤1: 清理环境                          │
│  kill_all_processes()                   │
│  ├─ 关闭 steam.exe                      │
│  ├─ 关闭 TslGame.exe (PUBG)            │
│  └─ 关闭所有相关进程                     │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  步骤2: Steam 客户端登录                 │
│  steam_login(username, password)        │
│  ├─ steam.exe -login <user> <pass>      │
│  ├─ wait_for_steam_ready()              │
│  │   ├─ 检测 steam.exe 进程             │
│  │   ├─ 检测 Steam 窗口 (win32gui)      │
│  │   ├─ 区分登录页/商店页                │
│  │   └─ 只有在商店页才算就绪             │
│  └─ 返回 True/False                     │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  步骤3: 启动 PUBG 游戏                   │
│  launch_pubg_game()                     │
│  ├─ steam.exe -applaunch 578080         │
│  ├─ 等待 5 秒让游戏初始化                 │
│  ├─ 阶段1: 检测协议窗口                   │
│  │   └─ check_for_agreement_window()    │
│  │       ├─ OpenCV 全屏搜索              │
│  │       ├─ 相似度 >= 50% 认为存在        │
│  │       └─ 返回 True/False              │
│  ├─ 如果有协议窗口:                      │
│  │   └─ activate_and_click_accept()     │
│  │       ├─ 激活窗口到前台               │
│  │       ├─ OpenCV 精确定位              │
│  │       ├─ 相似度 >= 95% 才点击          │
│  │       └─ 最多尝试 3 次                │
│  ├─ 阶段2: 等待游戏启动                   │
│  │   └─ 检测 TslGame.exe 进程           │
│  └─ 最多等待 50 秒                        │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  步骤4: 游戏运行阶段                     │
│  游戏运行 90 秒                          │
│  ├─ 每 30 秒显示进度                     │
│  └─ 倒计时显示                           │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  步骤5: 关闭游戏                          │
│  close_pubg_game()                      │
│  └─ taskkill /F /IM TslGame.exe         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│  步骤6: 关闭 Steam                       │
│  close_steam_client()                   │
│  ├─ steam.exe -shutdown (优雅关闭)       │
│  ├─ taskkill /F /IM steam.exe (强制关闭) │
│  └─ 等待并验证所有进程已关闭              │
└─────────────────────────────────────────┘
              ↓
        返回 True/False
```

### 5️⃣ 关键技术点

#### A. Steam 登录检测 (窗口检测)

```python
def wait_for_steam_ready(self, timeout=60):
    """
    多层次检测策略:
    1. 进程检测: steam.exe 是否存在
    2. 窗口检测: 是否有 Steam 窗口
    3. 标题验证: 窗口标题包含 "Steam"/"商店"/"Store"
    4. 状态判断: 区分登录页 vs 商店页
    """

    # 使用 win32gui 枚举所有窗口
    win32gui.EnumWindows(callback, steam_windows)

    # 分析每个窗口
    for hwnd, title in steam_windows:
        if "登录" in title:
            # Steam 还在登录页
            # 继续等待...
        elif "商店" or "Store" in title:
            # Steam 已就绪，在商店页
            return True
```

**为什么需要这个检测？**
- Steam 进程存在 ≠ Steam 已登录
- 需要等待用户完成 Steam Guard 验证
- 需要确认 Steam 加载到商店页面才能启动游戏

#### B. 协议窗口检测 (OpenCV)

```python
def check_for_agreement_window(self):
    """
    检测是否有"最终用户许可协议"窗口

    使用 OpenCV 模板匹配:
    1. 截取全屏
    2. 使用 accept_button.png 作为模板
    3. cv2.matchTemplate() 进行匹配
    4. 相似度 >= 50% 认为窗口存在
    """
    screen_gray = cv2.cvtColor(screen, cv2.COLOR_BGR2GRAY)
    template = cv2.imread('accept_button.png', cv2.IMREAD_GRAYSCALE)

    result = cv2.matchTemplate(screen_gray, template, cv2.TM_CCORR_NORMED)
    min_val, max_val, min_loc, max_max_loc = cv2.minMaxLoc(result)

    similarity = max_val  # 0-1 范围

    if similarity >= 0.50:
        return True  # 检测到协议窗口
    else:
        return False  # 没有协议窗口
```

**为什么需要这个检测？**
- 不是所有账号都有协议窗口
- 只在窗口存在时才点击，避免误操作
- 先检测再决定是否处理

#### C. 高精度点击 (OpenCV)

```python
def activate_and_click_accept(self):
    """
    使用 OpenCV 精确定位并点击"接受"按钮

    置信度要求: 95%
    方法: TM_CCORR_NORMED
    """
    # 1. 激活窗口到前台
    activate_window_by_title("许可")

    # 2. OpenCV 模板匹配
    result = cv2.matchTemplate(screen_gray, template, cv2.TM_CCORR_NORMED)
    similarity, location = cv2.minMaxLoc(result)

    # 3. 高置信度才点击
    if similarity >= 0.95:
        center_x = location[0] + template_width // 2
        center_y = location[1] + template_height // 2
        pyautogui.click(center_x, center_y)
        return True
    else:
        return False  # 置信度不够，不点击
```

**为什么需要 95% 置信度？**
- 避免误点击相似但不是目标的按钮
- 确保点击的是真正的"接受"按钮
- 保护账号安全

### 6️⃣ 错误处理机制

```python
try:
    # 执行流程
    result = bot.visit_pubg_page(account)

except Exception as e:
    # 捕获异常
    print(f"⚠️  PUBG 流程出错: {e}")

finally:
    # 无论如何都清理环境
    bot.kill_all_processes()
```

**每个步骤都有错误处理:**
- Steam 登录失败 → 清理环境，跳过该账号
- PUBG 启动失败 → 清理环境，跳过该账号
- 协议处理失败 → 继续尝试（最多3次）
- 游戏未启动 → 等待超时后跳过

### 7️⃣ 完成阶段

```python
# 所有账号处理完成
print("✅ 所有账号处理完成！共处理 {total} 个账号")

# GUI 显示
messagebox.showinfo("完成", "所有账号处理完成！")
```

---

## 🎯 运行示例

### 输入 (accounts.txt)
```
89599811	35935445	YSdqSVVK@744368.ljjmail.com	742001	https://mail.pubgs.team
```

### 输出 (控制台)
```
============================================================
🎮 Steam 账号批量登录脚本
============================================================
✅ 成功加载 1 个账号

📋 进度: [1/1]
📋 处理账号: 89599811

==================================================
🎮 开始 PUBG 游戏流程
==================================================
🧹 清理所有进程...
✅ 所有进程已清理

🔐 使用 Steam 客户端登录: 89599811
📍 执行: steam.exe -login 89599811 ********
⏳ 等待 Steam 完全准备就绪...
   ⚠️  检测到登录页面: 登录 Steam
   ✅ 检测到商店页面: Steam Store
✅ Steam 已就绪 (在商店页面, 耗时 8 秒)
✅ Steam 登录完成

🎮 启动 PUBG 游戏...
📍 使用 Steam -applaunch 启动游戏...
   执行: steam.exe -applaunch 578080
⏳ 等待游戏启动...

💡 阶段1: 检查是否有'最终用户协议'窗口...
   协议窗口检测相似度: 99.05%
   ✅ 检测到'最终用户协议'窗口
   尝试点击'接受'按钮 (1/3)...
   📊 最佳匹配相似度: 99.05%
   ✅ 相似度满足要求
   ✅ 成功点击'接受'按钮
   ✅ 游戏已启动

💡 阶段2: 继续等待游戏进程启动...
✅ PUBG 游戏已启动 (TslGame.exe 检测到，总耗时 12 秒)

⏳ 游戏运行中 (90 秒)...
   ⏱️  剩余时间: 60 秒
   ⏱️  剩余时间: 30 秒
   ✅ 游戏运行时间结束

🎮 关闭 PUBG 游戏...
✅ PUBG 游戏已关闭

🚪 正在关闭 Steam 客户端...
   💡 尝试优雅关闭 Steam...
   ✅ Steam 已完全关闭（耗时 3 秒）

✅ PUBG 流程完成

============================================================
✅ 所有账号处理完成！共处理 1 个账号
============================================================
```

---

## 🔑 关键特点

1. **智能检测**: 区分 Steam 登录页和商店页
2. **图像识别**: OpenCV 高精度识别协议窗口
3. **安全可靠**: 95% 置信度才点击，避免误操作
4. **完整清理**: 每个账号前后都彻底清理进程
5. **友好提示**: 详细的进度和状态信息
6. **错误处理**: 每个步骤都有异常处理

---

## 📊 技术栈

| 技术 | 用途 |
|------|------|
| Python 3.8+ | 主要编程语言 |
| pyautogui | 截图和鼠标点击 |
| OpenCV | 图像识别和模板匹配 |
| pywin32 | Windows 窗口检测 |
| tkinter | GUI 界面 |

---

*最后更新: 2026-03-06*
*版本: v2.3 (窗口检测版)*
