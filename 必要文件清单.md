# 必要文件清单

## 📋 最小化运行需求

### 🔴 必需文件（不能缺少）

#### 1. 核心脚本（二选一）
**使用 GUI**（推荐小白）:
- ✅ `src/steam_login_gui.py` (32 KB)
- ✅ `src/login_steam.py` (48 KB) - GUI 会调用它

**只用命令行**（进阶用户）:
- ✅ `src/login_steam.py` (48 KB) - 直接运行

#### 2. 图像识别模板
- ✅ `src/accept_button.png` (2.2 KB) - **非常重要！**
- 这是 OpenCV 图像识别的核心模板，不能缺少！

#### 3. 账号文件
- ✅ `accounts.txt` - 你的 Steam 账号列表

#### 4. Python 依赖
- ✅ `requirements.txt` - Python 库列表
- 需要运行 `pip install -r requirements.txt` 安装

---

### 🟡 重要文件（强烈推荐）

#### 1. 启动脚本
- ✅ `start.bat` - 最简单的启动方式

#### 2. 配置文件
- ✅ `config.json` - GUI 配置（如果使用 GUI）

---

## 📦 不同使用方式的必要文件

### 方式 1：使用 GUI（图形界面）

**必需文件**：
```
D:\SteamTool\
├── src\
│   ├── steam_login_gui.py        # GUI 主程序 ✅
│   ├── login_steam.py             # 被调用 ✅
│   └── accept_button.png          # 图像识别模板 ✅
├── accounts.txt                   # 账号列表 ✅
├── requirements.txt               # Python 依赖 ✅
├── config.json                    # GUI 配置 ✅
└── start.bat                      # 启动脚本 ✅
```

**使用方法**：双击 `start.bat`

---

### 方式 2：使用命令行（无需 GUI）

**必需文件**：
```
D:\SteamTool\
├── src\
│   ├── login_steam.py             # 主程序 ✅
│   └── accept_button.png          # 图像识别模板 ✅
├── accounts.txt                   # 账号列表 ✅
├── requirements.txt               # Python 依赖 ✅
└── start.bat                      # 可选，用于启动 GUI
```

**使用方法**：
```bash
python src\login_steam.py
```

---

## 🗑️ 可选文件（可以删除）

### 辅助工具（不需要）
- `src/chinese_to_tab.py` - 账号格式转换工具
- `src/config_accounts.py` - 交互式配置工具
- `src/quick_add.py` - 快速添加工具
- `src/visualize_match.py` - 模板测试工具

### 文档文件（不需要运行）
- `README.md` - 使用说明
- `CLAUDE.md` - 开发记录
- `图像识别配置完成.md` - 技术文档
- `项目运行逻辑说明.md` - 运行逻辑

### 测试文件（不需要）
- `test_images_temp/` - 测试图片
- `test_results/` - 测试结果

---

## ✅ 总结：最少需要什么文件？

### 如果使用 GUI（推荐）
```
最小文件集合：
├── src\steam_login_gui.py
├── src\login_steam.py
├── src\accept_button.png
├── accounts.txt
├── requirements.txt
└── start.bat
```

### 如果只用命令行
```
最小文件集合：
├── src\login_steam.py
├── src\accept_button.png
├── accounts.txt
└── requirements.txt
```

---

## 🎯 快速检查清单

运行前，确保这些文件存在：

- [ ] `src/login_steam.py` - 主脚本
- [ ] `src/accept_button.png` - **图像识别模板（重要！）**
- [ ] `accounts.txt` - 你的账号
- [ ] `requirements.txt` - Python 依赖

如果使用 GUI：
- [ ] `src/steam_login_gui.py` - GUI 界面
- [ ] `config.json` - GUI 配置
- [ ] `start.bat` - 启动脚本

---

## 💡 建议

**最简单的做法**：
1. 保留所有文件
2. 双击 `start.bat` 启动
3. 使用 GUI 操作

**最小化做法**：
1. 只保留核心文件（上面列出的）
2. 运行 `python src\login_steam.py`

---

**最后更新**: 2026-03-08
